---
description: 每次对仓库的修改完成后，必须留出用户核对（check）环节后再视为收尾。
alwaysApply: true
---

# 改后需用户核对（Review gate）

本仓库约定：**助手完成代码/文档/SKILL 等修改后，不得默认「用户已验收」**。

1. **收尾前**：用简短列表说明 **改了哪些路径**、**核心变更一句**、若有 **口径/日期/鉴权** 等敏感点须点明。
2. **明确要求用户 check**：请用户自行过一遍 diff、跑相关脚本、或对指标口径确认；在用户表示核对通过前，不把任务描述为「已可上线/已最终定稿」类结论。
3. 若用户这轮仅要求「打开/记录」本规则、尚无具体改动，仍遵守上述习惯，避免静默收尾。
4. 返回的文字需要按点阐述，文字说明不要涩口，利于用户理解
