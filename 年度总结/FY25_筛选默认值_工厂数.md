# FY25 筛选默认值 — 工厂数（dt_custom_chart_info）

## FY25 代表周（与财年脚本一致）

- **2026-03-23** ~ **2026-03-29**
- **有更新**：`dt_custom_chart_info.updated_at` 或 `dt_custom_dashboard.updated_at` 落在该周内。

## 指标

| 模块 | 全量工厂数（当前有效配置） | FY25 代表周内有更新的工厂数 |
|------|---------------------------|------------------------------|
| 自定义报表（type=1） | **4368** | **176** |
| 智能看板（type=3） | **15888** | **400** |

## 口径

- **默认值**：`filter_condition` JSON 中至少一条含 `valueInfo`，且 `conditionValue` 或 `conditionName` 为非空 JSON 数组。
- **未删除**：`COALESCE(deleted_at,0)=0`（图表与看板均判断）。
- **工厂**：`org_id`，ADB 01/02/03 结果并集去重。
- **注意**：部分**图表**的数据筛选也写在 `filter_condition`；与「仅筛选组件」不完全等价，需 `chart_type` 时产品线可再收窄。

详细 JSON：`FY25_筛选默认值_工厂数.json`
